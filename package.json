{
  "name": "fnutil",
  "version": "0.1.8",
  "description": "ES6 Functional Utilities",
  "main": "./",
  "repository": "https://github.com/f5io/fnutil",
  "engines": {
    "node": ">=4.0.0"
  },
  "files": [
    "index.js", "core", "decorators", "observable", "utils"
  ],
  "scripts": {
    "compile": "babel --stage 0 --out-dir ./ src",
    "test": "npm run compile && babel-tape-runner test/**/*.js | tap-bail | tap-difflet",
    "watch": "watch 'npm test' src test",
    "prepublish": "npm test"
  },
  "author": "Joe Harlow",
  "license": "MIT",
  "devDependencies": {
    "babel": "^5.5.6",
    "babel-runtime": "^5.5.6",
    "babel-tape-runner": "^1.2.0",
    "tap-bail": "0.0.0",
    "tap-difflet": "^0.3.0",
    "tape": "^4.2.0",
    "watch": "^0.16.0"
  }
}
